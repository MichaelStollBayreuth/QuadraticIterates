% Macros used only by the web version. They must have a counterpart in macros/print.tex.

% A Lean identifier. The html version wraps by itself, so no break hints are needed here.
\newcommand{\lname}[1]{\texttt{#1}}
